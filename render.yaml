services:
  - type: web
    name: diet-tracker
    env: node
    plan: starter
    repo: https://github.com/JulianJua/Diet-Tracker
    buildCommand: apk add --no-cache python3 make g++ sqlite-dev && npm install --build-from-source=sqlite3 && npm run build
    startCommand: node server/index.js
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_PATH
        value: /data/database.sqlite
      - key: UPLOADS_DIR
        value: /data/uploads
      - key: JWT_SECRET
        generateValue: true
    disk:
      name: data
      mountPath: /data
      sizeGB: 1


